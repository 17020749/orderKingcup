name: Tighten external export atomicity

on:
  pull_request:
    branches:
      - dev
    types:
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  patch-and-test:
    if: github.head_ref == 'agent/external-export-no-inventory'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source branch
        uses: actions/checkout@v4
        with:
          ref: agent/external-export-no-inventory
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply atomicity patch
        run: node scripts/tighten-external-export-atomicity.mjs

      - name: Commit patched source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Require atomic external release status updates"
          git push origin HEAD:agent/external-export-no-inventory

      - name: Install dependencies
        run: npm ci

      - name: Run client regression test
        run: node --test tests/external-export-release.client.test.mjs

      - name: Run Firestore rules regression test
        run: npx firebase emulators:exec --only firestore "node --test --test-concurrency=1 tests/external-export-release.rules.test.mjs"
