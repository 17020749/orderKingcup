name: Trigger V7.4 patch from pull request

on:
  pull_request:
    branches:
      - feature/v7.3-print-and-warehouse-search
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout V7.4 branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Setup Java for Firestore emulator
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Apply guarded V7.4 patch
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          source = Path('.github/workflows/apply-v7.4.yml').read_text(encoding='utf-8')
          start = "          python - <<'PY'\n"
          end = "\n          PY\n"
          if start not in source or end not in source:
              raise SystemExit('Không tìm thấy khối patch V7.4 trong workflow nguồn.')
          block = source.split(start, 1)[1].split(end, 1)[0]
          lines = [line[10:] if line.startswith('          ') else line for line in block.splitlines()]
          script = '\n'.join(lines) + '\n'
          old_helper = """from pathlib import Path

          def replace_once(path, old, new):
              file_path = Path(path)
              text = file_path.read_text(encoding='utf-8')
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{path}: expected one match, found {count}\\n--- expected ---\\n{old}')
              file_path.write_text(text.replace(old, new, 1), encoding='utf-8')
          """
          new_helper = """from pathlib import Path
          import re

          def replace_once(path, old, new):
              file_path = Path(path)
              text = file_path.read_text(encoding='utf-8')
              count = text.count(old)
              if count == 1:
                  file_path.write_text(text.replace(old, new, 1), encoding='utf-8')
                  return
              if count == 0:
                  parts = [re.escape(part) for part in re.split(r'\\s+', old.strip()) if part]
                  pattern = r'\\s+'.join(parts)
                  matches = list(re.finditer(pattern, text, flags=re.MULTILINE))
                  if len(matches) == 1:
                      match = matches[0]
                      file_path.write_text(text[:match.start()] + new + text[match.end():], encoding='utf-8')
                      return
              raise SystemExit(f'{path}: expected one match, found {count}\\n--- expected ---\\n{old}')
          """
          if old_helper not in script:
              raise SystemExit('Không tìm thấy helper replace_once trong patch nguồn.')
          Path('/tmp/apply-v7.4.py').write_text(script.replace(old_helper, new_helper, 1), encoding='utf-8')
          PY
          python /tmp/apply-v7.4.py

      - name: Commit V7.4 source changes
        shell: bash
        run: |
          rm -f .github/workflows/apply-v7.4.yml .github/workflows/trigger-v7.4-pr.yml v7.4-patch-error.log v7.4-verification-error.log
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: stabilize v7.4 print invoice and inventory"
          git push origin "HEAD:${{ github.event.pull_request.head.ref }}"

      - name: Install dependencies
        shell: bash
        run: |
          set +e
          timeout 600 npm ci --ignore-scripts --no-audit --no-fund --prefer-offline > /tmp/v7.4-verification.log 2>&1
          code=$?
          cat /tmp/v7.4-verification.log
          exit $code

      - name: Build
        run: npm run build | tee -a /tmp/v7.4-verification.log

      - name: Generate static output
        run: npm run generate | tee -a /tmp/v7.4-verification.log

      - name: Firestore rules tests
        run: npm run test:rules | tee -a /tmp/v7.4-verification.log

      - name: Record verification failure
        if: failure()
        shell: bash
        run: |
          test -f /tmp/v7.4-verification.log || echo "Verification failed before command output was available." > /tmp/v7.4-verification.log
          cp /tmp/v7.4-verification.log v7.4-verification-error.log
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f v7.4-verification-error.log
          git commit -m "chore: record v7.4 verification failure" || true
          git push origin "HEAD:${{ github.event.pull_request.head.ref }}" || true
